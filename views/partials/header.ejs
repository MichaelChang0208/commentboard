<nav>
        <a type="button" class="btn btn-danger" href="/posts">留言列表</a>
        <% if(user) { %>
        <a type="button" class="btn btn-info" href="/users/<%= user.id %>">我的帳戶</a>
        <% } else { %>
        <a type="button" class="btn btn-primary" href="/users/login">登入 / 註冊</a>
        <% } %>
</nav>
